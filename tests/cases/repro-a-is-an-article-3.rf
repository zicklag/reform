$ rule x_is_y
    (
        - sentence $( $a1 )? $x $( $a2 )? is $( $a3 )? $y
        $( $a1 is article )?
        $( $a2 is article )?
        $( $a3 is article )?
    )
    (
        $x is $y
    )

an is article

# Regression: this was not working, but would work if we
# defined `dummy is an article` above.
a an is article

$ assert an is article
$ assert a is article