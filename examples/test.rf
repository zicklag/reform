$ rule (parse after rule statement)
    (
        - sentence $after $action : $( $( $first_words )+ ;  )** $( $last_words )+
    )
    (
        rule (after $action rule)
            (
                after enacting $action
            )
            (
                $(
                    sentence $( $first_words )+
                )**
                sentence $( $last_words )+
            )
    )

Carry-out test:
    say `hi`;
    say `bye`

$ facts