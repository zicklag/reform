# Test: Full game playthrough
$ load ../game.txt

# Start in Garden
$ assert (here, Garden)
$ assert (in, key, Garden)
$ assert (in, lamp, Kitchen)
$ assert (in, book, Study)

# Where am I
> where am I
$ assert (here, Garden)

# Look around
> look
$ assert (described, Garden)

# Take key and go to Kitchen
> take key
$ assert (carrying, key)
$ assert not (in, key, Garden)

> go north
$ assert (here, Kitchen)
$ assert (described, Kitchen)

# Can't go back south? Actually we can
> go south
$ assert (here, Garden)

# Go back north
> go north
$ assert (here, Kitchen)

# Take lamp — it's bolted, can't take it
> take lamp
$ assert not (carrying, lamp)
$ assert (in, lamp, Kitchen)

# Go east to Hallway
> go east
$ assert (here, Hallway)
$ assert (described, Hallway)

# Try invalid direction
> go down
$ assert (here, Hallway)

# Go north to Study
> go north
$ assert (here, Study)
$ assert (described, Study)

# Take book
> take book
$ assert (carrying, book)
$ assert not (in, book, Study)

# Inventory should show key and book (lamp is bolted)
> inventory
$ assert (carrying, key)
$ assert (carrying, book)

# Drop everything in Study
> drop key
$ assert (in, key, Study)
$ assert not (carrying, key)

> drop lamp
$ assert not (carrying, lamp)

> drop book
$ assert (in, book, Study)
$ assert not (carrying, book)

# Inventory should be empty
> inventory
$ assert not (carrying, key)
$ assert not (carrying, book)
