[package]
name = "reform-wasm"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
reform = { path = ".." }
wasm-bindgen = "0.2"
tracing = { version = "0.1", default-features = false, features = ["std"] }
js-sys = "0.3"
